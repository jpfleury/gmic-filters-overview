# shellcheck shell=bash
# shellcheck disable=SC2034

# Don't edit this file. Instead, create a "user/inc/config.sh" file
# and override variables there. It will be included automatically.

# Output image file extensions
anim_ext=".gif"
anim_ext_settings=",10,0"
append_ext=".png"
append_ext_settings=""
merge_ext=".png"
merge_ext_settings=""
static_ext=".jpg"
static_ext_settings=",85"

# Timeout duration before stopping a command that takes too long to finish
timeout_duration="10s"
